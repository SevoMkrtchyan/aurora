spring:
  application:
    name: cloud-eureka-client

  cloud:
    config:
      discovery:
        enabled: true
        service-id: cloud-config-server
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
